import React from 'react'
import "./Search.css"

function Search(props) {
    return (props.serach) ? (
        <div className='search-history'>
            watch
        </div>
    ) : " ";
}

export default Search
